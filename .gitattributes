# Ensure consistent text encoding
* text=auto

# Prevent diffing large binary or compressed files
*.zip -diff
*.png -diff
*.jpg -diff
*.woff -diff
*.pdf -diff
*.x5 filter=lfs diff=lfs merge=lfs -text
