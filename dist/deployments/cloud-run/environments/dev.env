# Development environment configuration for integration-gateway

# Node.js environment
NODE_ENV=development

# Server configuration
PORT=8080
LOG_LEVEL=debug

# Authentication
JWT_SECRET=dev-jwt-secret-replace-in-production
JWT_EXPIRATION=1d

# Service specific configuration
MAX_CONCURRENT_REQUESTS=50
REQUEST_TIMEOUT_MS=30000

# Integration endpoints (replace with actual dev endpoints)
AGENT_SERVICE_URL=https://dev-agent-service.example.com/api/v1
AUTHENTICATION_SERVICE_URL=https://dev-auth-service.example.com/api/v1

# Cloud SQL configuration (if used)
DB_HOST=localhost
DB_USER=dbuser
DB_PASSWORD=dbpassword
DB_NAME=integration_gateway_dev

SERVICE_ACCOUNT=drlucyautomation@api-for-warp-drive.iam.gserviceaccount.com
