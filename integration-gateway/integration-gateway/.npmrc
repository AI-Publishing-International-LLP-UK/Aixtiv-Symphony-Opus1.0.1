# Suppress engine warnings during Docker builds
engine-strict=false
# Use legacy peer deps resolution for better compatibility
legacy-peer-deps=true
# Reduce verbosity of npm output
loglevel=warn
# Skip optional dependencies that might cause issues
optional=false
